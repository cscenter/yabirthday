package service;

import javax.ws.rs.Path;
import javax.ws.rs.Produces;

/**
 * Created by olya on 05.04.15.
 */

@Path("/group/")
@Produces("text/json") //"application/json" ?
public class GroupApiService {
/*
    @GET
    @Path("/")
    public String getList() {

    }

    @GET
    @Path("/{name}/")
    public String getListOfMembers(@PathParam("name") String name) {

    }

    @GET
    @Path("/{name}/{login}")
    public String getMember(@PathParam("name") String name, @PathParam("login") String login) {
        //это копия getUser. Нужен этот метод?
    }

    @PUT
    @Path("/{name}/{login}/")
    public Response addUser(@PathParam("name") String name, @PathParam("login") String login) {
        //что возвращать тут
        //не знаю вообще, как PUT писать... разберусь, когда можно будет тестить, надеюсь
    }
*/
}
